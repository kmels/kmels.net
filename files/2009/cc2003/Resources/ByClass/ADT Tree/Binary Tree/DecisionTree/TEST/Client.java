import java.io.*;import java.util.*;public class Client {	private static	BufferedReader inputStream = new BufferedReader(new InputStreamReader(System.in));	public static void main(String[] args) 	{		GuessingGame myGame = new GuessingGame("Is it in North America?", "Brazil", "U. S. A.");				do		{			System.out.println("Think of a country and I will guess it.");						myGame.play();						System.out.println("Play again? ");		} while (isUserResponseYes());		System.out.println("Bye!");	}  // end main	public static boolean isUserResponseYes()	{		String response = null;		boolean result = false;				try		{			response = inputStream.readLine();		}		catch (IOException e)		{			System.out.println("Error in reading input.");		}		response = response.toLowerCase();			if (response.equals("yes"))			 result = true;					else if (response.equals("no"))			 result = false;					else		{			System.out.println("Answer yes or no.");			result = isUserResponseYes();		} // end if				return result;	} // end isUserResponseYes		public static String getUserResponse()	{		String response = null;				try		{			response = inputStream.readLine();		}		catch (IOException e)		{			System.out.println("Error in reading input.");		}		return response;	} // end getUserResponse	public static void display(DecisionTree tree)	{		Iterator traverser = tree.getLevelOrderIterator();		while (traverser.hasNext())			System.out.println(traverser.next());	} // end display}  // end Client/* Initial tree:		Is it in North America?		  /               \	  Brazil		         U.S.A.*/