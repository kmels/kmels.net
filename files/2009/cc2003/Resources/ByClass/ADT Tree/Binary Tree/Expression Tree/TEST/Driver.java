import java.io.*;import java.util.*;public class Driver {	public static void main(String[] args) 	{		ExpressionTree a = new ExpressionTree();		a.setTree("a");		ExpressionTree b = new ExpressionTree();		b.setTree("b");		ExpressionTree c = new ExpressionTree();		c.setTree("c");		ExpressionTree d = new ExpressionTree();		d.setTree("d");		ExpressionTree e = new ExpressionTree();		e.setTree("e");				// building from bottom up		ExpressionTree multiply2 = new ExpressionTree();		multiply2.setTree("*", c, d);		ExpressionTree add = new ExpressionTree();		add.setTree("+", b, multiply2);				ExpressionTree multiply1 = new ExpressionTree();		multiply1.setTree("*", a, add);				ExpressionTree myTree = new ExpressionTree();		myTree.setTree("/", multiply1, e);				System.out.println("--------------------");		System.out.println("evaluate results in : " + myTree.evaluate() + " s/b 23");		System.out.println("Done.");	}  // end main}  // end Driver