import java.util.*;public class Date implements Comparable{	private int year;	private int month;	private int day;		public Date(int year, int month, int day)	{		this.year = year;		this.month = month;		this.day = day;	} // end constructor		public int compareTo(Object other)	{		Date localOther = (Date) other;		if (year < localOther.year)			return -1;		else if (year > localOther.year)			return 1;		else if (month < localOther.month) // same year			return -1;		else if (month > localOther.month)			return 1;		else if (day < localOther.day)	// same month			return -1;		else if (day > localOther.day)	// same month			return 1;		else			return 0;	} // end compareTo	public String toString()	{		return (new Integer(month)).toString() + "/" + (new Integer(day)).toString()  + "/" + (new Integer(year)).toString();		} // end toString} // end Date