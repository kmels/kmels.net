public Object next() throws NoSuchElementException{	if (hasNext())	{		Node returnNode = currentNode;  // get current node		currentNode = currentNode.next; // advance iteration		return returnNode.data;         // return data in current node	}	else		throw new NoSuchElementException("Iteration has ended; " +	                                 "illegal call to next.");} // end next