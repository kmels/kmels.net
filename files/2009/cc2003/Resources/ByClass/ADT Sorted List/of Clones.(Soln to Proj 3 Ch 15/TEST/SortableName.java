/** This class is like the class Name, but implements Sortable. * For simplicity, we have not involved the class ImmutableName. */public class SortableName implements Sortable{	private String first;  // first name	private String last;   // last name		public SortableName()	{	} // end default constructor		public SortableName(String firstName, String lastName)	{		first = firstName;		last = lastName;	} // end constructor	public Object clone()	{		try		{			SortableName theCopy = (SortableName) super.clone();			return theCopy;		}		catch (CloneNotSupportedException e)		{			throw new Error(e.toString());        }	} // end clone		public int compareTo(Object other)	{		int result = this.last.compareTo(((SortableName)other).last);				if (result == 0)		{   		    // last names are equal; check first			result = this.first.compareTo(((SortableName)other).first);		} // end if 				return result;		} // end compareTo	public void setName(String firstName, String lastName)	{		first = firstName;		last = lastName;	} // end setName	public String getName()	{		return toString();	} // end getName	public void setFirst(String firstName)	{		first = firstName;	} // end setFirst		public String getFirst()	{		return first;	} // end getFirst		public void setLast(String lastName)	{		last = lastName;	} // end setLast	public String getLast()	{		return last;	} // end getLast	public String toString()	{		return first + " " + last;	} // end toString	} // end SortableName