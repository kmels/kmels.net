import java.io.*;import java.util.*;public class Driver{	public static void main(String[] args) 	{		Concordance wordTable  = new Concordance();		String fileName = "Data.txt"; // could be read		try		{			BufferedReader dataFile = new BufferedReader(new FileReader(fileName));			wordTable.readFile(dataFile);		} 		catch (IOException e)		{			System.out.println("I/O error " + e);		}				wordTable.display();				System.out.println("\n\nDone.");	}  // end main	}  // end Driver/*1: learning without thought is labor lost; 2: thought without learning is perilous.is  1 2 labor  1 learning  1 2 lost  1 perilous  2 thought  1 2 without  1 2 */