import java.io.*;import java.util.*;public class Driver{	public static void main(String[] args) 	{		FrequencyCounter wordTable  = new FrequencyCounter();		String fileName = "Data.txt"; // file name could be read		try		{			FileReader reader = new FileReader(fileName);  // to shorten line			BufferedReader dataFile = new BufferedReader(reader);			wordTable.readFile(dataFile);		} 		catch (IOException e)		{			System.out.println("I/O error " + e);		}				wordTable.display();				System.out.println("\n\nDone.");	}  // end main	}  // end Driver